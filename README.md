# Ntxuva
A simple game of ntxuva
Its only starting, so the is not much to explore now

## **Console**
* `npm install`
### **build the project**
* `npm run build`

### **run tests (optional)**
* `npm test`

### **serve the game**
* `npm run serve`

